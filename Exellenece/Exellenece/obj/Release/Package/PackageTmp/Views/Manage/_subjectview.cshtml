@using PagedList.Mvc
@model Exellenece.Models.Subject1
    @{
        Layout = null;
    }
     
    <div class="card-body table-responsive" style="display: block;">
        <!--bigtable -->
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th width="14%">Sr.No.</th>
                    <th width="64%">Subject Name</th>
                    <th width="9%">Status</th>
                    <th width="7%">Edit</th>
                    <th width="6%">Remove</th>
                </tr>
            </thead>
            <tbody>
                @{int i = 0;
                    int a = Model.SubjectList1.PageNumber;
                    if (a == 0)
                    {
                        a = 1;
                    }
                    else
                    {
                        a = ((a * 50) - 49);
                    }
                }
                @foreach (var item in Model.SubjectList1)
            {

                    <tr>
                        <td>@a</td>
                        <td>@item.Subject_Name</td>

                        <td><a href="#"><strong>@item.Status</strong></a> </td>
                        <td><a href="/Manage/Subject/@item.Id?command=ed" class="red">Edit</a></td>
                        <td><a href="/Manage/Subject/@item.Id?command=del"><i class="fa fa-trash"></i></a></td>


                    </tr>
                    a = a + 1;
                }

            </tbody>
        </table>

        <div class="pagination-outer float-right paging">
            @Html.PagedListPager(Model.SubjectList1, page1 => Url.Action("Subject", new { page1 }))
        </div>
    </div>